[build]
builder = "nixpacks"
buildCommand = "cd server && pip install uv && uv sync"

[deploy]
startCommand = "cd server && uv run uvicorn server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
